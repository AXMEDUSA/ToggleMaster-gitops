apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: observability
type: Opaque
stringData:
  admin-user: admin
  # Alterar antes do deploy — nunca commitar senha real
  admin-password: "ChangeMe@2024!"
